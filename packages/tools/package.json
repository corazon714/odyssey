{
  "name": "@odyssey/tools",
  "version": "0.0.0",
  "private": true,
  "description": "Build- and dev-time tooling: sim harness, content linter, image pipeline, i18n checks.",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
