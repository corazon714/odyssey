/**
 * Zod schemas for the content pack. These are the authoritative definition of the
 * content model — if docs/engine-spec.md and this directory disagree, the schema is
 * right and the doc is a bug (CLAUDE.md 9).
 *
 * Intentionally empty in Phase 0. Event, Choice, Outcome, Effect, Predicate and the
 * four registries (modifiers, complications, universal-choices, quirks) land in Phase 1.
 */

export {};
