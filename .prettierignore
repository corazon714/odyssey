node_modules/
dist/
build/
coverage/
reports/
.expo/
pnpm-lock.yaml
packages/content/images/generated/
apps/mobile/ios/
apps/mobile/android/

# Regenerated by `expo start` on every run, without a trailing newline, and gitignored by
# Expo's own apps/mobile/.gitignore. Left tracked here it made `pnpm format:check` fail for
# anyone who had run the dev server. Expo owns this file; Prettier does not police it.
apps/mobile/expo-env.d.ts

# Hand-authored reference doc. Excluded so Prettier does not rewrite prose and table
# layout nobody asked it to touch. Remove this line to opt it in.
docs/engine-spec.md

# Generated sim output, pinned for review. Prettier would reflow the fixed-width report and
# make `pnpm sim:diff` permanently noisy against freshly generated output.
docs/sim-baseline.md
